# Producción — reemplazar por la URL real del backend desplegado.
# Nunca poner secretos acá: todo lo VITE_* queda expuesto en el bundle del cliente.
VITE_API_BASE_URL=https://api.tu-dominio.com/api
VITE_USE_MOCKS=false
